<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>fr.openent</groupId>
        <artifactId>app-parent</artifactId>
        <version>1.2.0</version>
    </parent>

    <groupId>org.entcore</groupId>
    <artifactId>blog</artifactId>
    <version>5.4.7-patched</version>

    <scm>
        <connection>scm:git:https://github.com/edificeio/blog.git</connection>
        <developerConnection>scm:git:https://github.com/edificeio/blog.git</developerConnection>
        <url>https://github.com/edificeio/blog</url>
    </scm>

    <repositories>
        <repository>
            <id>ode</id>
            <name>ODE Repository</name>
            <url>https://maven.opendigitaleducation.com/nexus/content/groups/public</url>
        </repository>
    </repositories>

    <properties>
        <entCoreVersion>6.14.9-patched</entCoreVersion>
        <junitVersion>4.13.2</junitVersion>
        <explorerVersion>2.5.7</explorerVersion>
        <modMongoVersion>4.1.1</modMongoVersion>
    </properties>

    <dependencies>
        <dependency>
            <groupId>io.vertx</groupId>
            <artifactId>vertx-core</artifactId>
            <version>${vertxVersion}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.entcore</groupId>
            <artifactId>common</artifactId>
            <version>${entCoreVersion}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.vertx</groupId>
            <artifactId>vertx-mongo-client</artifactId>
            <version>${vertxVersion}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junitVersion}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.entcore</groupId>
            <artifactId>test</artifactId>
            <version>${entCoreVersion}</version>
            <classifier>fat</classifier>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.opendigitaleducation</groupId>
            <artifactId>explorer</artifactId>
                <version>${explorerVersion}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.opendigitaleducation</groupId>
            <artifactId>explorer</artifactId>
            <version>${explorerVersion}</version>
            <classifier>tests</classifier>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.gatling.highcharts</groupId>
            <artifactId>gatling-charts-highcharts</artifactId>
            <version>${gatlingHighchartsVersion}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.vertx</groupId>
            <artifactId>mod-mongo-persistor</artifactId>
            <version>${modMongoVersion}</version>
            <scope>runtime</scope>
            <classifier>fat</classifier>
        </dependency>
    </dependencies>

    <profiles>
        <!--
            org.entcore:tests ne fournit QUE les scénarios Gatling/Scala (src/test/scala/.../BlogScenario.scala).
            Depuis le 08/09/2026, le module `tests` d'entcore est exclu de la publication du fork
            (open-ent/entcore-v2, commit 275a060 : maven-scala-plugin 2.15.2 ne compile plus Scala 2.12
            sous JDK 21), donc org.entcore:tests:6.14.9-patched n'existe plus sur GitHub Packages.
            Maven résout les dépendances de portée `test` même avec -Dmaven.test.skip=true : laissée
            dans <dependencies>, elle faisait échouer `mvn package` (donc toute la CI).
            On l'isole ici : `mvn -Pgatling-it ...` pour rejouer les scénarios de charge le jour où
            l'artefact sera republié.
        -->
        <profile>
            <id>gatling-it</id>
            <dependencies>
                <dependency>
                    <groupId>org.entcore</groupId>
                    <artifactId>tests</artifactId>
                    <version>${entCoreVersion}</version>
                    <scope>test</scope>
                </dependency>
            </dependencies>
        </profile>
    </profiles>
</project>
